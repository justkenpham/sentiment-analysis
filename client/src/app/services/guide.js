// "services" is the place to store all the fetching or operation templates from the client to the server

// Example

/**
import * as httpClient from "./BaseService";
import Path from "../constants/PathUrl";

const getAllUsers = () => {
  return httpClient.get(Path.USER_ALL);
};

export { getAllUsers };
*/